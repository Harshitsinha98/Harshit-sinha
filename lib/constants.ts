// Intro loading-screen duration. Shared by the loading screen and the
// navbar entrance animation so they stay in sync from a single value.
export const LOADING_DURATION_MS = 2800;
export const LOADING_DURATION_S = LOADING_DURATION_MS / 1000;
